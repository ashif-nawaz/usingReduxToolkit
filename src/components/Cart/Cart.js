import React from "react";
import Section from "../UI/Section";
import Products from "./Products";


const Cart = (props) => {
  return (
    <Section>
      <Products />
    </Section>
  );
};

export default Cart;
